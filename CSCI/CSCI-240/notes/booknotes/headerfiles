-- Preface -- 
This document will include all the library includes that were discussed in the document "booknotes".


<iostream>
    access to input/output


<iomanip>
    access to input/output manipulation


<string>
    access to 
        std::string data type
        

<cstdint>
    access to fixed width integer types
        

<typeinfo>
    access to 
        typeid().name()


<cstdlib>
    access to 
        rand
        srand
        EXIT_SUCCESS
        EXIT_FAILURE
        string/numeric conversions


<unistd.h> (POSIX api)
    access to 
        sleep

<ctime>
    access to 
        time(0)

<cmath>
    access to mathematical functions
        trig stuff
        Exponential and logarithmic functions
        Power functions
        rounding 
        etc

<algorithm> 
    access to certain algorithms such as:
        std::sort
        std::binarysearch(pointerfistvaluearr,pointerlastvaluearr,value)

<memory>
    access to smart pointers
        unique_ptr
        shared_ptr
        weak_ptr

<cstring>
    used for cstring functions
    strlen
    strcat
    strcpy
    strcmp
    strncat
    strncpy

<limits>
    access to functions for numeric types
        max
        min 

<sstream>
    access to string streams

<utility>
    for std::pair

<chrono>
  unsigned seed = std::chrono::system_clock::now().time_since_epoch().count();

<random>
    std::default_random_engine engine(seed);

<vector> 
    for std::vector

<array> 
    for std::aray

<list>
    for std::list

<set>
    for std::set<T>

<map> 
    for std::map

<cstdarg>
    va_list, va_start, va_arg, and va_end for variadic functions (ellipsis)

<numeric> 
    for partial_sum

<functional>

<initializer_list>
    used for init list function parameters

