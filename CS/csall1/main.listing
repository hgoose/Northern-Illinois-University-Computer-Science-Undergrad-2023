                if |$IR[11-10] = 00$| then
                    if |$AC < 0$| then
                        |$PC \leftarrow PC + 1$|
                    endif

                else if |$IR[11-10] = 01$|
                    if |$AC = 0$| then
                        |$PC \leftarrow PC + 1$|
                    endif

                else if |$IR[11-10] = 10$| then
                    if |$AC > 0$| then
                        |$PC \leftarrow PC + 1$|
                    endif
                endif

