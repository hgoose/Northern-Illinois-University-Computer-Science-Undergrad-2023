            e = 1
            // b!=0 and b>=0 (nonnegative b) implies b>0
            while (b>0) {
                if (b & 0x01) {
                    e = e * a
                }
                a = a*a
                b>>1 // Divide by two
            }
