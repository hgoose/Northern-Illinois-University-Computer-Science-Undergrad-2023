digraph SimpleTree {
    // Set a circular shape for nodes
    node [shape=circle, ];

    A
    B
    C
    D

    D [style=filled, color=red]

    // Define the tree structure
    A -> {B C};
    B -> {D E};
    C -> {F G};
}

