function horner(x::AbstractVector, c)
    n = length(c) - 1
    p = c[n+1]*ones(length(x))
    for j = n:-1:1
        p .= p.*x .+ c[j]
    end
    p # Return p
end
horner(x::Real, c) = horner([x], c)[1]
